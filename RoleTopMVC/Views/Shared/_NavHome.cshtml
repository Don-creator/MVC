<nav>
    <div class = "Options">
        <link rel="stylesheet" href="~/CSS/Style.css">
        <link href="https://fonts.googleapis.com/css?family=Patua+One&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css?family=Teko&display=swap" rel="stylesheet">
        
        <ul>
            @if(string.IsNullOrEmpty(@Model.UsuarioEmail))
                    {
                        <!-- <li class="bt-common-menu">
                            <a  asp-controller="Cliente" asp-action="Index">Cadastro | Login</a> </li> -->
                    }
                    else
                    {
                        <li class="bt-common-menu">
                        <a asp-controller="Cliente" asp-action="Logoff">Logoff</a> </li>
                        <!-- Depois é necessario mudar o Historico para MinhaConta no view e controlador  -->
                        <li class="bt-common-menu">
                        <a asp-controller="Cliente" asp-action="Historico">Minha Conta</a> </li>
                    }

            @if(string.IsNullOrEmpty(@Model.UsuarioEmail))
            {
                <li class="bt-common-menu" ><a onclick="toggleMobileMenu()" asp-controller="Cliente" asp-action="Login" >Login</a></li>
            } 
            <!-- else if
            {
                <li class="bt-common-menu" ><a onclick="toggleMobileMenu()" asp-controller="Cliente" asp-action="Logoff">Logoff</a></li>
            }  -->
            
            <li class="bt-common-menu"><a onclick="toggleMobileMenu()" href='@Url.Action("Index", "Home")#Eventos'>Eventos</a></li>
            <li class="bt-common-menu"><a onclick="toggleMobileMenu()" href='@Url.Action("Index", "Home")#local'>Local</a></li>
            <li class="bt-Facebook"><a onclick="toggleMobileMenu()" href="https://www.facebook.com/Rol%C3%AATop-106506047400870/?modal=admin_todo_tour">Facebook</a></li>
        </ul>
    </div>
</nav>